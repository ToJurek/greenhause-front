

export enum APIData {
    TEMPERATURE_API,
    SUN_API,
    HUMIDITY_API,
    MOISTURE_API
}

export enum Sensor {
    TEMPERATURE_SENSOR,
    SUN_SENSOR,
    HUMIDITY_SENSOR,
    MOISTURE_SENSOR
}

export enum Actor {
    TEMPERATURE_ACTOR,
    SUN_SENSOR,
    HUMIDITY_SENSOR,
    MOISTURE_SENSOR
}